import React from "react";

function Nav({ handleSearchChange }) {
    return (
        <nav className = "navbar navbar-expand navbar-light bg-light">
            <div className="navbar-collpase row" id="navbarNav"></div>
        </nav>
    );
}

export default Nav;